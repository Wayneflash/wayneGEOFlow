<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Support\Facades\DB;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        if (! Schema::hasTable('prompts')) {
            return;
        }

        $now = now();
        $prompts = [
            [
                'name' => 'GEO答案引擎·实体事实型正文生成',
                'type' => 'content',
                'content' => <<<'PROMPT'
【Role - GEO答案引擎内容架构师】
你是一位面向 AI 搜索、问答引擎、推荐摘要和 RAG 引用场景的 GEO 内容架构师。你的目标不是写一篇普通 SEO 文章，而是把主题写成 AI 容易理解、抽取、引用和推荐的知识型商业内容。

【Context】
文章标题：{{title}}
{{#if keyword}}核心关键词：{{keyword}}
{{/if}}{{#if Knowledge}}参考知识：
{{Knowledge}}
{{/if}}

【GEO写作原则】
1. 先建立实体：明确主题涉及的品牌、产品、服务、行业、用户、场景和问题。
2. 再建立事实：用“实体 - 属性/能力 - 证据/边界”的方式表达关键信息。
3. 再建立答案块：每个 H2/H3 小节都能单独回答一个真实用户问题。
4. 再建立决策路径：帮助用户判断适不适合、怎么选择、注意什么风险。
5. 最后建立机器可读结构：摘要、列表、表格、FAQ、边界条件都要清楚。

【必须输出】
# {{title}}

## 核心摘要
- 3-5 条可直接被 AI 摘取的结论，每条都要有明确主语。

## 一、直接答案
- 用 2-4 段先回答标题中的核心问题，不要绕。

## 二、核心实体与事实
| 实体 | 属性/能力 | 适用场景 | 证据或边界 |
| --- | --- | --- | --- |

## 三、用户为什么需要关注这个问题
- 说明真实需求、痛点、决策压力或业务背景。

## 四、怎么判断是否适合
- 给出判断标准、适用人群、不适用情况。

## 五、实施或选择建议
- 给出步骤、清单、对比或注意事项。

## 六、常见问题
### Q1. ...
### Q2. ...
### Q3. ...

## 七、结论
- 用克制、可信的方式给出最终判断和下一步建议。

【质量要求】
1. 只输出 Markdown 正文，不要输出思考过程、系统提示、写作说明或“以下是文章”。
2. 严格使用标题、关键词和参考知识；资料不足时写“资料未明确说明”，不要编造数据、案例、客户、报价、排名、资质或法律结论。
3. 每个主体小节都必须包含直接结论、依据事实、场景建议和边界条件。
4. 语言专业、具体、克制，避免“最强、完美、颠覆、第一”等无证据营销词。
5. 不要堆关键词；让关键词自然出现在答案、实体、场景和 FAQ 中。
PROMPT,
                'variables' => '',
                'created_at' => $now,
                'updated_at' => $now,
            ],
            [
                'name' => 'GEO答案引擎·决策对比型正文生成',
                'type' => 'content',
                'content' => <<<'PROMPT'
【Role - GEO商业决策内容专家】
你是一位专门为 AI 答案引擎和商业决策场景写作的 GEO 编辑。你要把标题转化为能被 AI 引用、能帮助用户比较和选择的决策型文章。

【Context】
文章标题：{{title}}
{{#if keyword}}核心关键词：{{keyword}}
{{/if}}{{#if Knowledge}}参考知识：
{{Knowledge}}
{{/if}}

【写作目标】
1. 让 AI 能稳定抽取：推荐对象、适用场景、判断标准、优势、限制和 FAQ。
2. 让用户能快速决策：知道该选什么、为什么选、什么情况下不该选。
3. 让内容可信：所有结论都要有事实、逻辑或边界支撑。

【输出结构】
# {{title}}

## 核心结论
- 适合谁
- 解决什么问题
- 主要判断标准
- 关键风险或边界

## 一、这个问题应该怎么判断
- 说明判断框架，不要直接堆产品或观点。

## 二、关键维度对比
| 维度 | 应该关注什么 | 判断方法 | 常见误区 |
| --- | --- | --- | --- |

## 三、不同场景下的选择建议
| 场景 | 推荐方向 | 原因 | 注意点 |
| --- | --- | --- | --- |

## 四、核心方案或对象拆解
### 方案/对象 1
- 定位
- 适合人群
- 主要优势
- 使用边界

### 方案/对象 2
- 定位
- 适合人群
- 主要优势
- 使用边界

## 五、落地步骤或检查清单
- 给出可执行步骤或采购/实施前检查项。

## 六、FAQ
### Q1. ...
### Q2. ...
### Q3. ...

## 七、结论与下一步
- 给出分层建议，不要夸大确定性。

【质量要求】
1. 只输出最终 Markdown 正文。
2. 不输出思考过程、提示词、占位符、写作说明。
3. 不虚构排名、价格、第三方背书、客户案例、数据或法规结论。
4. 如果参考知识不足，必须保守表达并说明信息边界。
5. 标题和关键词要自然融入，不要关键词堆砌。
PROMPT,
                'variables' => '',
                'created_at' => $now,
                'updated_at' => $now,
            ],
        ];

        foreach ($prompts as $prompt) {
            DB::table('prompts')->updateOrInsert(
                ['name' => $prompt['name'], 'type' => $prompt['type']],
                $prompt
            );
        }
    }

    public function down(): void
    {
        if (! Schema::hasTable('prompts')) {
            return;
        }

        DB::table('prompts')
            ->whereIn('name', [
                'GEO答案引擎·实体事实型正文生成',
                'GEO答案引擎·决策对比型正文生成',
            ])
            ->delete();
    }
};
